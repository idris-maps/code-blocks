import { Renderer } from '@code-blocks/types'
import { Transformer } from 'unified'
declare const _default: (renderer: Renderer, languages: string[]) => (tree: any) => Promise<Transformer> 
export default _default;